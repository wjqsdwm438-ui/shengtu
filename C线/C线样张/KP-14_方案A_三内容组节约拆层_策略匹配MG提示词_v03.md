# KP-14｜方案A_三内容组节约拆层｜策略匹配MG提示词 v03

## 状态

- 待用户审核新构图；未批准正式生图。
- v01线框、v02线框及v02静态候选图均已驳回，不得作为参考。
- 不修改T线合同。

## 用户指导的显示偏差

- 主层级只显示四字标签：`标签输出`、`策略匹配`、`业务价值`，不在三个区域继续展开T线三行长句。
- 这与T线“三行正文必显”存在显式偏差；当前仅按用户指导进行构图审核，T线文件保持不变。

## 新构图

- 标题：`智能分类：匹配策略`。
- 内容组1：`标签输出`，包含`常温刚需型`、`生鲜时效型`、`更细配送类型`。
- 内容组2：`策略匹配`，包含中央抽象判断节点、全部无交叉连线，以及`普通`、`加急`、`冷链`三个出口。
- 内容组3：`业务价值`，只用抽象图示表达减少错配，不展开长句。
- 参考线框：`KP-14_三内容组节约拆层_结构线框参考_v03.png`。

## 保守拆层计划

1. `L0_固定底板`：L2背景、主体框和标题。
2. `L1_标签输出`：左侧整组，不再拆三枚标签。
3. `L2_策略匹配`：右侧整组，包含判断节点、全部无交叉连线和三个策略出口；不单独拆线头、箭头或各条连线。
4. `L3_业务价值`：底部整组。

总计**4层**，其中1层固定、3层动画内容。

## 动画顺序

`L1_标签输出` → `L2_策略匹配` → `L3_业务价值`。

## 构图验收

- 只有三个内容组，无额外长句信息条。
- 判断节点、全部连线和策略出口归入同一组。
- 无交叉线、无回环、无单独线头图层。
- 不使用`116.png`、v01/v02线框或历史静态候选图。

