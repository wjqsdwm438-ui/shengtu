# KP-14｜方案A_参考锁定版｜策略匹配MG提示词 v01

> **驳回状态：禁止执行。** 2026-08-26用户驳回当前结构线框，原因为版式不便拆分制作动画。`KP-14_标签判断三向扇出_结构线框参考_v01.png`仅作驳回记录，不得附加、不得生图。待新版动画拆分骨架重新审核后另建v02文件。

## 执行状态

- 本文件仅供用户审核；未经单页确认不调用生图模型。
- 单页范围：KP-14。不批量生图，不修改T线合同。
- 用户于2026-08-26明确否决`116.png`，并批准制作KP-14中性结构线框参考。执行时只允许L2原始母版和该中性线框；不附加任何课程成品页或历史生图结果。

## 原文语义核对卡

1. **原文对应段落**

   “第一，智能产品分类体系，实现精准配送定位。传统物流靠人工主观判断产品运输需求，分类模糊、容易出错。数字化系统可搭建智能标签体系，自动根据产品保质期、运输温控要求、时效需求、市场属性，精准划分常温刚需型、生鲜时效型两大品类，同时细分普通配送、加急配送、冷链配送等细分类型，自动匹配对应的运输配送策略，从源头避免策略错配。”

2. **课程叙事作用**

   承接KP-13已形成的智能标签，完成“品类判断→配送策略匹配”，收束智能分类方法的输出与业务价值。

3. **第一层核心结论**

   智能标签用于识别产品品类及更细配送类型，系统再自动关联普通、加急或冷链配送方案。

4. **第二层证据、动作或后果**

   品类判断的输出包括常温刚需型、生鲜时效型和更细配送类型；策略匹配的结果是减少分类错误，避免产品与配送模式错配。

5. **必须锁定的因果、映射或先后顺序**

   标签输出 → 品类/类型判断 → 普通、加急或冷链策略匹配 → 减少错误与错配。不能将配送方案倒置为产品标签的输入。

6. **不应进入画面的背景信息**

   KP-13的保质期/温控/时效/市场属性输入过程、企业案例、具体货物案例、算法逻辑、准确率、真实系统界面、未冻结配送类型、品牌、数值和英文缩写。

7. **原文与T线之间的抽象、改写或风险差异**

   - 原文将“品类划分、细分类型、策略匹配”放在同一段；T线依据分页逻辑只保留KP-14的判断和匹配输出，与KP-13形成输入/输出分工。
   - T线的“从源头减少分类错误，避免产品与配送模式错配”是对原文“从源头避免策略错配”的教学化展开，因果一致。
   - 原文明确为“普通配送、加急配送、冷链配送”；T线精简为“普通、加急或冷链等配送方案”。画面可复用“普通”“加急”“冷链”作标签，不可增加其他类型。
   - T线执行提示词仍有“生成×1080”宽度缺失与“主觉觉为”后缺文；本文件仅根据同页冻结的1920×1080输出设置和“标签生成与策略出口”主视觉补全运行表达，不修改T线。
   - T线原指定`116.png`，但用户审核认为其代码/表格属性不适合KP-14，已批准改用无文字、无课程内容的`KP-14_标签判断三向扇出_结构线框参考_v01.png`。这是用户批准的单页执行偏差，T线合同文件保持不变。

8. **专页骨架和动画分组顺序**

   - 骨架名称：**标签判断·三向策略扇出骨架**。
   - 唯一主视觉几何：左侧标签集合 → 中央紧凑判断开关 → 右侧普通/加急/冷链三向扇出。
   - 主连接数量：3条标签短连接汇入判断开关，再以3条短连接扇出三类策略，共**6条**；无交叉、无回环。
   - 动画顺序：标签输出 → 中央判断 → 三策略扇出 → 业务价值收束。

## 明确参考附件

- L2原始母版：`E:\shengtu\智能物流\样片版式\版式2背景+框+通用标题框.png`
- 用户批准的中性结构参考：`C:\Users\Administrator\.codex\worktrees\5a2d\skills-v2.7-development-snapshot-20260814\C线样张\KP-14_标签判断三向扇出_结构线框参考_v01.png`
- 不附加KP-13或任何其他课程成品页。

## 差异化精简运行提示词

```text
只执行KP-14“方案A_参考锁定版”。

以“版式2背景+框+通用标题框.png”为母版，严格保留1920×1080画布、L2科技蓝扁平背景、顶部通用标题框、半透明蓝色主体框、本课浅蓝/白/科技蓝配色和外层安全边界。删除旧标题，只替换为“智能分类：匹配策略”。

以“KP-14_标签判断三向扇出_结构线框参考_v01.png”仅锁定左侧三标签、中央判断节点、右侧三策略扇出的空间关系和6条主连接；线框中的空白占位不是成品样式，不需逐像素复制。视觉语言为现代职业教育MG关键帧：科技蓝主色，青/绿/橙少量强调，扁平免抠物流图示，干净描边，轻微投影，清楚中文层级和适度留白。

主体采用“标签判断·三向策略扇出骨架”，形成左聚合、中判断、右扇出的单一主视觉；不得复制KP-13的2×2属性输入组、中央扫描柱或左输入右标签的同比例构图。

左侧用“常温刚需型”“生鲜时效型”“更细配送类型”组成三枚轻量品类标签，标签大小可错位，禁止排成2×2卡片。三条短连接汇入中央紧凑的抽象判断开关，判断开关不使用仪表盘、软件按钮或伪算法界面。

从中央判断开关向右侧明确扇出3条不交叉短连接，分别通向“普通”“加急”“冷链”三个配送方案出口，三个出口呈扇形展开，不等宽、不堆成三列卡片。全页主连接准确为3条汇入线加3条扇出线，共6条。

页面底部用一条低视觉重量的业务价值信息条收束，不参与主连线。

必显文字，逐字一致：

标题：
智能分类：匹配策略

正文：
标签输出：识别常温刚需型、生鲜时效型及更细配送类型
策略匹配：自动关联普通、加急或冷链等配送方案
业务价值：从源头减少分类错误，避免产品与配送模式错配

标签只能复用正文已有原词。

禁止：KP-13的2×2属性输入漏斗、三列等宽、三卡片并排、代码、表格、仪表盘、真实或伪造企业系统、软件品牌、账号、算法准确率、新增配送类型、交叉线、回环、具体数值、英文缩写、额外文字、数字、品牌、水印及母版漂移。

输出：1920×1080，high，opaque，PNG。
```

## 动画分组顺序

1. 左侧三枚品类标签错位入场。
2. 三条汇入线出现，中央判断开关点亮。
3. 普通、加急、冷链三条策略连接依次扇出。
4. 右侧三个配送方案出口依次出现。
5. 底部业务价值信息条出现。

## 验收门槛

- 唯一主视觉为“左标签→中判断→右三策略扇出”，不复制KP-13骨架。
- 只有3条汇入线和3条扇出线，无交叉、无回环。
- 只出现普通、加急、冷链三类策略，无额外类型。
- 无真实或伪造企业系统、算法准确率、代码、表格或软件控件。
- 标题与三行正文逐字正确，标签只复用正文原词。
