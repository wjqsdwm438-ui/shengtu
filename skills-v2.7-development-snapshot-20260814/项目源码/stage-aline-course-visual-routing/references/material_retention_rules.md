# Material Retention Rules

When real material is supplied, decide preserve/package/replace/style-migrate. Real evidence should not be redrawn unless user allows AI redraw. Failure images are diagnosis input, not style references.
