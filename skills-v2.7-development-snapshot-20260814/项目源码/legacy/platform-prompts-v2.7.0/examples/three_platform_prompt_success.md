# Example: Expanded Three-Platform Prompt Success

Use this example only when the user explicitly requests multi-platform, 即梦, nanobanana, full prompt, complete prompt package, or all platform prompts.

Success: the T-line output includes 即梦中文, Image2 English, and nanobanana English. Each platform includes full/short/negative/revision fields, and English platforms contain no Chinese.

Default behavior is different: for normal runs, T-line outputs only one Image2 English concise package with `image2_execution_prompt`, `negative_prompt`, and compact `revision_prompt_template`.
