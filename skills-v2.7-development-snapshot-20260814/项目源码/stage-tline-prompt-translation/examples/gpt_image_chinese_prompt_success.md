# 成功示例：GPT Image 中文提示词

输入是一个由用户明确选择并记录确认的完整课程视觉页方案。模型建议或评分不能替代该选择。

成功输出只包含一套 `GPT_Image中文合同`：目标能力为 GPT Image，请求模型为 `gpt-image-2`，实际执行模型为 `未报告`；中文执行提示词继承冻结布局与素材政策；中文负面约束只列硬禁项；背景为 `opaque`；中文局部修改模板完整。

成功关键：写入前已先提议产物与交接方案并得到本次授权；合同没有生成英文版本或历史平台版本，没有把请求模型写成已确认的实际执行模型，也没有引入未经用户确认的视觉方向。合同完成后立即停止，没有自动进入 C线或生图。
