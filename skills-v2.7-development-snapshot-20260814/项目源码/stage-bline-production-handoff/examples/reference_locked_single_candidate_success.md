# Reference-Locked Single Candidate Success

Input: the user provides a reference folder, requests the same layout with new exact course text, and confirms the selected primary reference.

Success: B-line routes to A16, emits only `方案A_参考锁定版`, preserves real reference paths and template version, freezes verbatim text, requires C-line attachment, and marks exploration `not_applicable`.
