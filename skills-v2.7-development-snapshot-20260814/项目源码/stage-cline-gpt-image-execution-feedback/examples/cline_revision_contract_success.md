# 成功示例：C线局部修改合同

用户要求减轻完整课程视觉页的边框厚重感。C线先复述对当前单一修改方向的理解，用户确认后才准备局部修改合同；写入前另行提议产物与交接方案，并只询问是否按该方案执行。合同保留标题区、图片数量、素材封装、背景不透明和视觉样张状态；`仅修改项` 只包含边框粗细；`禁止修改项` 阻止整页重绘。若要执行生图，还必须针对当前这一张图另行取得明确授权，合同确认本身不构成生图授权。若执行工具没有返回模型 ID，`实际执行模型` 保持 `未报告`。
