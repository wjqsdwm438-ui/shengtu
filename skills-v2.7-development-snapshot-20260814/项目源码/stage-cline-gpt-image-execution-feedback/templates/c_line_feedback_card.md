# C线 GPT Image 执行反馈卡

```yaml
状态: C线GPT Image执行反馈
候选方案ID:
目标能力: GPT Image
请求模型: gpt-image-2
实际执行模型: 未报告
使用的中文提示词:
输出设置:
  尺寸:
  质量:
  背景: opaque / transparent / auto
  格式: png / webp
  透明背景状态: 不适用 / 预览能力已请求 / 已降级为不透明
执行前偏差预测:
  可能漂移:
  抑制结构:
  禁止写入图像:
  冻结项:
视觉样张状态: visual_sample_generated
结果标签: 视觉样张
结论: 通过 / 可局部修 / 退回B线 / 退回A线
失败类型:
局部修改合同:
  修订范围:
  保留项:
  仅修改项:
  禁止修改项:
  参考依据:
  最小修改指令:
  需要用户确认: yes / no
用户确认状态: pending / confirmed / rejected
```
